import { ThemedText, ThemedView } from "@/components/theme";

export default function Account(): JSX.Element {
    return (
        <ThemedView className="pt-20">
            <ThemedText content="Account" />
        </ThemedView>
    );
}
